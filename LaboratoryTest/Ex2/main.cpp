#include <iostream>
#include <cmath>
#include <fstream>

using namespace std;
ifstream f("date.in");

int main()
{
    int k, n, v[n];
    f >> k;
    f >> n;
    int iter = 0;
    while(f >> v[iter]) {
        iter++;
    }

    //subsir maxim de zigzag



}
